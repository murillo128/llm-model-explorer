---
name: repository-bootstrap-completion-marker
description: Bootstrap publication is complete; required repository labels are intentionally pending manual creation.
---

# Bootstrap completion marker

The project-specific `README.md` and `AGENTS.md` have been published to `main`.

The repository owner explicitly chose to create the required Skillforge workflow labels manually later. Do not rerun repository bootstrap or modify project files from this marker.

Once the required labels exist and are verified, this file can be deleted. Its presence records only that final manual label setup remains; it is not an executable bootstrap procedure.
